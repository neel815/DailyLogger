{
  "name": "daily-standup-logger",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/server.js",
    "postinstall": "npm --prefix backend install"
  },
  "engines": {
    "node": ">=18"
  }
}
